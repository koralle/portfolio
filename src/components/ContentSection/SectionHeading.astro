---
import { SectionHeading as SectionHeadingView } from '../../ui/ContentSection';

const { id: _ignoredId, ...props } = Astro.props;
---

<SectionHeadingView {...props}>
  <slot />
</SectionHeadingView>
